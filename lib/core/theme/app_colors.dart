import 'package:flutter/material.dart';

/// Placeholder color palette for new projects.
///
/// ⚠️ Replace these with your project-specific brand colors.
class AppColors {
  // Primary brand color (edit per project)
  static const Color primary = Colors.blue;

  // Accent color (edit per project)
  static const Color accent = Colors.orange;

  // Light mode colors
  static const Color lightBackground = Colors.white;
  static const Color lightSurface = Color(0xFFF5F5F5);

  // Dark mode colors
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
}
